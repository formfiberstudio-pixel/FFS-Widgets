{
  "name": "00_ffs_widgets",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:travel": "npm run dev --workspace=apps/travel-log",
    "dev:creator": "npm run dev --workspace=apps/creator-timeline",
    "build:all": "npm run build --workspaces"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}