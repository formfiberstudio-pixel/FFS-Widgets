# Copy this to .env and fill in real values before running with --live.
# Never commit the real .env file.
#
# Which sub-goals route to which Notion database lives in sync-routes.json
# (copy sync-routes.example.json), not here — a single token can push to any
# number of databases as configured there.

NOTION_TOKEN=secret_...
